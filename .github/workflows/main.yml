name: FakeShop
run-name: "FakeShop ${{ github.run_id }}"

on: 
  push:
    branches:
      - 'main'
  workflow_dispatch:

jobs:
 CI:
  runs-on: ubuntu-latest
  steps:
  - run: echo "First"
    name: "Primeiro"

 CD:
  runs-on: ubuntu-latest
  steps:
  - run: "echo CD"
  

